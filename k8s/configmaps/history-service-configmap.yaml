apiVersion: v1
kind: ConfigMap
metadata:
  name: history-service-config
  namespace: peerprep
data:
  # Server Configuration
  NODE_ENV: 'production'
  PORT: '8004'

  # Google Cloud Configuration
  GCP_PROJECT_ID: 'peerprep-475911'
  USE_SECRET_MANAGER: 'true'

  # Logging
  LOG_LEVEL: 'info'

  # CORS Configuration (update after getting domain/IP)
  CORS_ORIGIN: 'http://34.8.234.19,http://localhost:3000'

  # Note: DB credentials and JWT secret are fetched from Secret Manager:
  # - history-service-db-connection-string (PostgreSQL connection)
  # - user-service-jwt-secret (shared JWT secret)
